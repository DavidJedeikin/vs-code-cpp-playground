{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Clang++",
            "type": "shell",
            "command": "bear -- clang++ -std=c++20 main.cpp -o output -O0 -g ",
            "detail": "Builds the source code",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build"
            },
        }
    ]
}