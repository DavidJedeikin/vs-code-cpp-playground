{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/output",
            "preLaunchTask": "Build with Clang++",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}